#include "main.h"

/**
 * _strcat - function that concantenates two strings
 * @src: input values
 * @dest: input value
 *
 * Return: (void)
 */
char *_strcat(char *dest, char *src)
{
	int i;
	int l;
	i=0;
	for (i = 0; dest[i] != '\0'; i++)
	{
	
	}
	
	for (l = 0; scr[l] != '\0'; l++)
	{
	
	}

	for (i=0; i <= l; i++)
	{
		dest[i + i] = src[i];
	}

	return (dest);
