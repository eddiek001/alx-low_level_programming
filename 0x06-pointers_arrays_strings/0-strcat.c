#include "main.h"

/**
 * _strcat - function that concantenates two strings
 * @src: input values
 * @dest: input value
 *
 * Return: (void)
 */
char *_strcat(char *dest, char *src)
{
	int s1, s2, i;

	for (s1 = 0; dest[s1] != '\0'; s1++)
	{
	}
	for (s2 = 0; scr[s2] != '\0'; s2++)
	{
	}
	for (i = 0; i <= s2; i++)
	{
		dest[s1 + i] = src[i];
	}
	return (dest);
